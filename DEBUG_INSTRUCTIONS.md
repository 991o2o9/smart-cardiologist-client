# 🔧 Отладка измерителя пульса

## 🚨 Проблема

После окончания времени измерения результат пульса не появляется.

## 🔍 Что добавлено для отладки

### 1. Отладочная информация в консоли

Откройте консоль браузера (F12) и следите за сообщениями:

```
Обрабатываем сигнал, длина: X
Найден пик на частоте: X Hz, BPM: X
Устанавливаем первый BPM: X
Передаем BPM в родительский компонент: X
Достигнут 100% прогресс, останавливаем измерение
Останавливаем измерение, текущий BPM: X
```

### 2. Визуальная отладочная информация

В интерфейсе теперь отображается:

- **Прогресс-бар** с процентами и секундами
- **Отладочная строка**: "Сигнал: X точек | BPM: X"
- **Кнопка ручной остановки** для принудительного завершения

### 3. Улучшенная обработка ошибок

- Если BPM не определен, показывается сообщение об ошибке
- Увеличено время для финальной обработки данных (1 секунда)

## 🧪 Как тестировать

### Шаг 1: Запуск с отладкой

```bash
yarn dev
```

### Шаг 2: Открыть консоль

- Нажмите F12 в браузере
- Перейдите на вкладку "Console"

### Шаг 3: Тестирование

1. Перейдите к вопросу о пульсе
2. Нажмите "Начать измерение"
3. Разрешите доступ к камере
4. Приложите палец к камере
5. **Следите за консолью** - должны появляться сообщения
6. Наблюдайте за отладочной информацией в интерфейсе

### Шаг 4: Анализ результатов

#### ✅ Если работает:

- В консоли: "Устанавливаем первый BPM: X"
- В интерфейсе: "BPM: X" в отладочной строке
- Результат появляется автоматически

#### ❌ Если не работает:

- В консоли: "Пик не найден в допустимом диапазоне частот"
- В интерфейсе: "BPM: —" в отладочной строке
- Появляется сообщение об ошибке

## 🔧 Возможные решения

### Проблема 1: Слабый сигнал

**Симптомы**: "Пик не найден в допустимом диапазоне частот"
**Решение**:

- Улучшите освещение
- Прижмите палец сильнее к камере
- Держите камеру неподвижно

### Проблема 2: Недостаточно данных

**Симптомы**: "Обрабатываем сигнал, длина: X" (где X < 16)
**Решение**:

- Увеличьте время измерения (измените `windowSec={15}` на `windowSec={20}`)
- Улучшите качество сигнала

### Проблема 3: BPM вне диапазона

**Симптомы**: "BPM вне диапазона: X (допустимо: 40-200)"
**Решение**:

- Проверьте, что измеряете пульс, а не другие движения
- Убедитесь, что палец прижат к камере

## 🎯 Ручная остановка

Если автоматическая остановка не работает:

1. Нажмите кнопку "Остановить измерение"
2. Проверьте консоль на наличие ошибок
3. Попробуйте изменить условия измерения

## 📊 Ожидаемые значения

### Нормальный пульс в покое:

- **60-100 BPM** для взрослых
- **40-60 BPM** для спортсменов
- **100-140 BPM** для детей

### Качество сигнала:

- **Хорошо**: 100+ точек сигнала
- **Удовлетворительно**: 50-100 точек
- **Плохо**: <50 точек

## 🚀 Быстрое исправление

Если проблема повторяется:

1. **Увеличьте время измерения**:

   ```tsx
   <PulseDetector
     windowSec={20} // вместо 15
     // ...
   />
   ```

2. **Уменьшите требования к сигналу**:

   ```tsx
   if (signal.length < Math.min(capacity * 0.2, 8)) return; // еще меньше
   ```

3. **Добавьте fallback значение**:
   ```tsx
   if (!bpm && signal.length > 10) {
     // используйте простой подсчет пиков
   }
   ```

## 📞 Поддержка

При возникновении проблем:

1. Скопируйте все сообщения из консоли
2. Сделайте скриншот отладочной информации
3. Опишите условия измерения (освещение, устройство, браузер)
